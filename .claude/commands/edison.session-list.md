---
description: "List sessions across states"
edison-generated: true
edison-id: "session-list"
edison-platform: "claude"
---

# edison.session-list

Lists sessions across all states. Use `--status` to filter (accepts semantic state or directory alias like `wip`).

```bash
edison session list
```


## When to use

When you need to discover sessions and where they live

## Related Commands
- /edison.session-status
- /edison.session-show
- /edison.session-next
