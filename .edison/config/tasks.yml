tasks:
  paths:
    # Happy Stacks project-owned artifact templates (NOT composed via `edison compose`).
    # These enforce mandatory frontmatter keys used by happy-stacks guards.
    template: ".edison/templates/happy-stacks/TASK.md"
    qaTemplate: ".edison/templates/happy-stacks/QA.md"
