packs:
  # Enable tech-aware overlays/validators for Happy component development.
  # NOTE: This controls Edison’s own composition/validation guidance; it does NOT
  # start any servers or change Happy Stacks runtime behavior by itself.
  active:
    - happy-stacks
    - expo
    - typescript
    - react
    - prisma
    - e2e-web
    # NOTE: do NOT enable tailwind unless this codebase uses Tailwind CSS (web) directly.

